import { SpicialDatePipe } from './spicial-date.pipe';

describe('SpicialDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SpicialDatePipe();
    expect(pipe).toBeTruthy();
  });
});
